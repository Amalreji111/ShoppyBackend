import { ResponseTransformInterceptor } from './interceptor.interceptor';

describe('InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTransformInterceptor()).toBeDefined();
  });
});
